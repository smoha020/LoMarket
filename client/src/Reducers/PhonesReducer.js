import Items from '../Items/PhoneItems';

const initState = {
    items: Items,
    bought: []
}

const ProductsReducer = (state = initState, action) => {
    return state;
}

export default ProductsReducer