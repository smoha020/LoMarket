import Items from '../Items/ShirtItems';

const initState = {
    items: Items,
    bought: []
}

const ShirtReducer = (state = initState, action) => {
    return state;
}

export default ShirtReducer