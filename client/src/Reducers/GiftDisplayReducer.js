import Items from '../Items/GiftDisplayItems';

const initState = {
    items: Items,
    bought: []
}


const GiftDisplayReducer = (state = initState, action) => {
    return state;
}

export default GiftDisplayReducer