import Items from '../Items/PantsItems';

const initState = {
    items: Items,
    bought: []
}

const PantsReducer = (state = initState, action) => {
    return state;
}

export default PantsReducer